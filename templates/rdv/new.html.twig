{% extends 'layouts/main.html.twig' %}
{% block title %}Nouveau rendez-vous{% endblock %}

{% block page_header %}
  <div class="d-flex align-items-center justify-content-between">
    <h4 class="fs-16 mb-0">Nouveau rendez-vous</h4>
    <a href="{{ path('app_rdv_index') }}" class="btn btn-ghost-secondary">
      <i class="ri-arrow-left-line"></i> Retour
    </a>
  </div>
{% endblock %}

{% block body %}
  <div class="card">
    <div class="card-body">
      {% include 'rdv/_form.html.twig' with { form: form, button_label: 'Créer' } %}
    </div>
  </div>
{% endblock %}
