{% extends 'layouts/main.html.twig' %}
{% block title %}Nouveau client | Velzon{% endblock %}

{% block page_header %}
<div class="row mb-3 pb-1">
  <div class="col-12 d-flex align-items-center justify-content-between">
    <div>
      <h4 class="fs-16 mb-1">Créer un client</h4>
      <p class="text-muted mb-0">Ajouter un nouveau client</p>
    </div>
    <a href="{{ path('app_client_index') }}" class="btn btn-soft-dark">
      <i class="ri-arrow-left-line align-middle me-1"></i> Retour
    </a>
  </div>
</div>
{% endblock %}

{% block body %}
<div class="card">
  <div class="card-body">
    {{ include('client/_form.html.twig', {'button_label': 'Enregistrer'}) }}
  </div>
</div>
{% endblock %}
