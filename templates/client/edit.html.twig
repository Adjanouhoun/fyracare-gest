{% extends 'layouts/main.html.twig' %}
{% block title %}Modifier client | Velzon{% endblock %}

{% block page_header %}
<div class="row mb-3 pb-1">
  <div class="col-12 d-flex align-items-center justify-content-between">
    <div>
      <h4 class="fs-16 mb-1">Modifier le client</h4>
      <p class="text-muted mb-0">{{ client }}</p>
    </div>
    <div class="d-flex gap-2">
      <a href="{{ path('app_client_show', {id: client.id}) }}" class="btn btn-soft-dark">
        <i class="ri-arrow-left-line align-middle me-1"></i> Retour
      </a>
      {{ include('client/_delete_form.html.twig') }}
    </div>
  </div>
</div>
{% endblock %}

{% block body %}
<div class="card">
  <div class="card-body">
    {{ include('client/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
  </div>
</div>
{% endblock %}
