{{ form_start(form, {'attr': {'class': 'needs-validation', 'novalidate': true}}) }}
<div class="row g-3">
  <div class="col-md-6">
    {{ form_label(form.nometprenom, 'Prénom', {'label_attr': {'class': 'form-label'}}) }}
    {{ form_widget(form.nometprenom, {'attr': {'class': 'form-control'}}) }}
    {{ form_errors(form.nometprenom) }}
  </div>
  <div class="col-md-6">
    {{ form_label(form.telephone, 'Téléphone', {'label_attr': {'class': 'form-label'}}) }}
    {{ form_widget(form.telephone, {'attr': {'class': 'form-control'}}) }}
    {{ form_errors(form.telephone) }}
  </div>
  <div class="col-md-6">
    {{ form_label(form.email, 'Email', {'label_attr': {'class': 'form-label'}}) }}
    {{ form_widget(form.email, {'attr': {'class': 'form-control'}}) }}
    {{ form_errors(form.email) }}
  </div>
    <div class="col-md-6">
    {{ form_label(form.notes, 'Notes', {'label_attr': {'class': 'form-label'}}) }}
    {{ form_widget(form.notes, {'attr': {'class': 'form-control'}}) }}
    {{ form_errors(form.notes) }}
  </div>
  {# 🔧 Boucle corrigée: for + if séparés #}
  {% for child in form %}
    {% if child.vars.name not in ['nometprenom','telephone','email','notes','_token'] %}
      <div class="col-md-6">
        {{ form_row(child) }}
      </div>
    {% endif %}
  {% endfor %}

  <div class="col-12 d-flex gap-2 justify-content-end mt-3">
    <a href="{{ path('app_client_index') }}" class="btn btn-soft-dark">Annuler</a>
    <button class="btn btn-primary">{{ button_label|default('Enregistrer') }}</button>
  </div>
</div>
{{ form_end(form) }}
